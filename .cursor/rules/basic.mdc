---
description: 
globs: 
alwaysApply: true
---
### 代码编辑规范
- 避免重复代码，提取共用逻辑到单独的函数或类
- 生成 git commit 信息请使用中文
- 新创建测试用例需放在项目根目录下的tests目录下
- 有一个依赖库能解决就不需要引入第二个，且这个依赖库必须安装
- golang 作为服务端，接口需要有测试用例
- 使用 gin框架，数据访问基于gorm以便以后可以容易的迁移到别的数据库类型